# 검증결과

- 현재 GitHub main `MaterialInputStatus.jsx`에서 `& .MuiTab-root`가 동일 sx 객체에 2회 선언된 것을 확인
- 뒤쪽 선언이 앞쪽 v52.48.5.44.78 보정값을 덮어쓰는 구조 확인
- 두 블록을 단일 `MuiTab-root` 스타일로 합치는 패치 작성
- 마지막 탭 `mr: 0.45` 추가
- 적용 스크립트 `node --check` 통과
- 더미 파일 1회 적용 및 재적용 확인
- Excel 다운로드 관련 코드 변경 없음
- Supabase SQL 없음
- 전체 Vite build / 실제 브라우저 렌더링은 이 환경에서 수행하지 않음
