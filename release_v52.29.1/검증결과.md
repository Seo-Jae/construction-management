# v52.29.1 검증결과

최신 GitHub main 기준:
- `src/Dashboard.jsx`는 v52.29 적용 전 기준 확인
- `src/page/HistoricalDailyReportUpload.jsx`도 v52.29 적용 전 기준 확인
- 과거 업로드 매핑은 기존 `경량 → 경량벽체`, `천정 → 세대천정`
- process도 기존에는 `mapLegacyJob()`을 사용 중

v52.29 실패 로그 분석:
- 모든 replaceOnce 단계를 통과
- `requiredHistorical`의 마지막 사후검증에서만 실패
- 원인: 실제 결과는 다중라인 ternary인데 한 줄 문자열로 검증

v52.29.1:
- 사후검증을 실제 다중라인 코드와 정확히 일치하도록 수정
- process 원본 보존 block 확인
- 기존 process `mapLegacyJob()` block이 남아 있으면 실패하도록 추가 검증
- 적용 전 모든 검증 완료 후에만 실제 파일 저장
- 적용 전 자동 백업
- 중복 적용 감지

SQL:
- 기존 정규화 규칙 동일
- backup table RLS 활성화
- anon/authenticated 직접 접근권한 제거
