# v52.20 검증결과

기준 운영 파일:
- `src/page/AttendanceWorkerPortal.jsx`
- Git blob SHA: `9d545b2c1b0622d74296519b82eb5e5bc89eaf9a`

확인한 기존 동작:
- 작업자 session token은 이미 `window.localStorage`에 저장됨
- 기존 서버 세션 생성 SQL은 `clock_timestamp() + interval '30 days'`
- 기존 worker resolver는 `expires_at > clock_timestamp()` 조건 사용
- 기존 작업자 로그아웃 RPC는 현재 세션 행을 DELETE함

적용 스크립트:
- 운영 파일 Git blob SHA 확인
- 예상 파일과 다르면 자동 중단
- 적용 전 자동 백업
- 중복 적용 감지
- 수정 후 필수 마커 검증

SQL:
- 기존 함수 전체를 덮어쓰지 않고 세션 테이블 trigger 방식 사용
- 향후 기존 로그인/가입 함수가 30일 값을 INSERT해도 DB에서 infinity로 보정
- 기존 기기 바인딩/토큰 해시 검증 구조 변경 없음

로컬 검증:
- 적용 스크립트 `node --check` 통과
- 실제 기준 파일에 1회 적용 성공
- 동일 파일에 재실행 시 중복 적용 감지
- 변경 후 JSX 대상 문자열 검증
- ZIP 무결성 검사 완료

실제 Supabase SQL 실행과 Vite Production build는 사용자 운영환경에서 최종 확인합니다.
