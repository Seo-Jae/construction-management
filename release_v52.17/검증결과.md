# v52.17 검증결과

- 기준 `AttendanceManagement.jsx` Git blob: `75c10cef603732d849c8a1b94aedbd7ac3abd934`
- 기준 `AttendanceWorkerPortal.jsx` Git blob: `fb41bb3dde8fd00c7c4ccfa7464588ac979f64f7`
- 두 기준 파일은 제작 시점 GitHub `main`과 일치함을 확인했습니다.
- v52.16 근로자관리 코드가 포함된 `AttendanceManagement.jsx`에서 직접 수정했습니다.
- v52.14.9 이후 근로자앱 중점위험요인/금월 출결 코드가 포함된 `AttendanceWorkerPortal.jsx`에서 직접 수정했습니다.
- 프론트에서 호출하는 v52.17 RPC 3개와 SQL 생성 함수명을 대조했습니다.
- `attendance_worker_me_v52_14`의 기존 worker/today_events/month_events/risk_broadcasts 응답을 보존하고 `announcements`만 추가했습니다.
- 적용 스크립트는 기준 SHA256 불일치 시 중단하고 적용 전 자동 백업합니다.
- SQL은 `begin/commit`, `lock_timeout`, `statement_timeout`을 사용하며 재실행 가능한 `create table if not exists`, `create or replace function` 방식입니다.
- 이 환경에서는 실제 Supabase DB 실행 및 프로젝트 `npm run build`는 수행할 수 없으므로 운영 프로젝트에서 최종 빌드 확인이 필요합니다.
