# 검증결과

- 현재 GitHub main의 `src/utils/systemGuidePopup.js`에서 두 문구가 공통 팝업 렌더링에 포함되어 있는 구조를 확인
- `공개 가이드` DOM 제거 패치 작성
- `사용 순서 한눈에 보기` 하단 안내문 DOM 제거 패치 작성
- 제목 `사용 순서 한눈에 보기`는 유지하도록 구성
- `가이드 전체 안내(선택)`의 summary 렌더링은 건드리지 않음
- 적용 스크립트 `node --check` 통과
- 더미 파일 기준 1회 적용 및 재적용(`[이미 적용됨]`) 확인
- Supabase SQL 변경 없음
- 실제 브라우저 인쇄 미리보기는 패키지 생성 환경에서 직접 실행하지 않음
