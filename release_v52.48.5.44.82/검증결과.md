# 검증결과

- 현재 GitHub main의 v52.48.5.44.81 `systemGuidePopup.js` 인쇄 CSS 기준으로 작성
- 현재 인쇄 CSS에는 `.details`에 페이지 나눔 규칙이 없어 제목만 앞 페이지에 남을 수 있는 구조 확인
- `.details { break-before: page; page-break-before: always; }` 추가
- 인쇄 시 `.details`를 block으로 변경
- `.detail-title` 뒤 페이지 나눔 방지 규칙 추가
- 적용 스크립트 `node --check` 통과
- 더미 파일 기준 1회 적용 및 재적용 확인
- Supabase SQL 변경 없음
- 실제 Chrome 인쇄 미리보기 렌더링은 패키지 생성 환경에서 직접 실행하지 않음
