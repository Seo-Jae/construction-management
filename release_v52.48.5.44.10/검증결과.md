# v52.48.5.44.10 검증결과

## 통과 항목

- `LaborCostManagement.jsx` Babel JSX 문법 검사 통과
- 사용자 제공 소스 전체 기준 Vite production build 통과
- 적용 스크립트 Node 문법 검사 통과
- `v52.48.5.44.9.5` 기준 파일에 신규 적용 성공
- 같은 적용 스크립트를 2회 실행해도 결과와 백업 개수가 변하지 않는 재실행 검증 통과
- 사용자 제공 원본에서 `v52.48.5.44.9` → `.9.1` → `.9.2` → `.9.3` → `.9.4` → `.9.5` → `.10` 전체 적용 순서 검증 통과
- 기준 해시와 다른 사용자 수정 파일을 교체하지 않는 안전 중단 검증 통과
- 화면 릴리스 해시 일치 확인

## 확인된 계산 규칙

- 복합공정 원 단가 5,800원에서 현재 공정 2,600원 입력 시 다른 공정 3,200원 자동 계산
- 배분합계 5,800원 / 차액 0원인 경우에만 저장 허용
- 현재 공정 적용 계약 노무비 = 현재 공정 배분단가 × 계약수량
- 연결 공정 또는 원 단가가 변경되면 기존 배분 스냅샷을 사용하지 않고 재배분 요구

## 배포 시 필수 확인

Supabase 연결 정보가 릴리스 환경에 포함되지 않아 SQL은 정적 검토까지 수행했습니다. 실제 배포 전 Supabase SQL Editor에서 `v52.48.5.44.10_labor_contract_allocations.sql` 전체 실행이 필요합니다.

사용자 제공 ZIP에는 `index.html`이 없으므로 빌드 검증 중에만 표준 Vite 진입 파일을 임시로 추가했고, 검증 후 제거했습니다. 릴리스 ZIP에는 임시 파일과 빌드 산출물을 포함하지 않았습니다.
