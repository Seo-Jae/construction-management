# v52.48.5.44.63 검증결과

- 기준 파일 SHA256 확인 완료
- `.62` 상태에서 가이드 관련 파일이 `.60/.55` 이후 변경되지 않은 기준으로 패치 작성
- 적용 스크립트 1회 적용 성공
- 동일 패치 2회째 재적용 시 `이미 적용됨` 처리 확인
- `guideAnnotationEditor.js` Node 구문 검사 통과
- `systemGuidePopup.js` Node 구문 검사 통과
- `guideCatalog.js` Node 구문 검사 통과
- `GuideAnnotatedImage.jsx` TypeScript JSX 파싱 검사 통과
- `Guide.jsx` TypeScript JSX 파싱 검사 통과
- 설명박스 도구(`data-mode="note"`) 존재 확인
- 설명박스가 번호 배지 렌더링에서 제외되는 것 확인
- 설명박스가 번호 재정렬/번호 연결 대상에서 제외되는 것 확인
- 작성 미리보기 및 공개 가이드에서 설명박스는 표시되고 번호 원은 표시되지 않도록 확인
- Supabase SQL 변경 없음

전체 Vite `npm run build`는 사용자 프로젝트 환경에서 최종 확인합니다.
