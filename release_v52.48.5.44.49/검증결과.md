# v52.48.5.44.49 검증결과

## 적용 스크립트
- v52.48.5.44.48 기준 복사본에 1회 적용: 성공
- 동일 복사본에 재적용: 성공(멱등성 확인)
- 적용 후 5개 대상 파일 SHA-256: 릴리스 해시와 일치

## 정적 검사
- `src/config/guideCatalog.js`: `node --check` 통과
- `src/utils/systemGuidePopup.js`: `node --check` 통과
- `src/utils/guideAnnotationEditor.js`: `node --check` 통과
- JSX 대상 파일은 변경 내용/태그 구조를 수동 확인

## 전체 Vite 빌드
현재 작업 컨테이너의 기존 `node_modules`에 Vite 실행 파일이 없어 `npm run build`는 이 환경에서 완료하지 못했습니다.
사용자 프로젝트의 정상 의존성 환경에서 적용 후 `npm run build`를 최종 확인합니다.

## 주요 호환성
- 기존 .48 annotation JSON은 새 필드가 없어도 normalize 단계에서 기본값을 자동 부여
- DB 컬럼 추가 없음(JSONB 내부 확장)
- 기존 공개/초안 데이터 유지
