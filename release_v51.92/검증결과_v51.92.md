# v51.92 검증결과

## 통과 항목

- `ExpenseResolution.jsx` JSX 번들 문법 검사
- 통행료 도착지·도착시간 `용인 14:00` 표시
- 유류대 경로·거리 `서울→용인 59km` 표시
- PDF 유류비 건별 입력 및 선택 항목 일괄 입력
- 유류비·통행료 별도 금액 계산 및 전체 합계
- 사용일·항목·사용내용·금액 셀 직접수정
- 사용내역 행 클릭 수정 팝업 제거
- 기존 km 저장값 읽기
- 유류비 포함 저장값을 다시 읽는 호환 처리
- 실제 엑셀 양식 생성 후 재열기
- 엑셀 갑지 B8 `서울→용인 59km`
- 엑셀 갑지 C8 유류비 `50,000`
- 엑셀 갑지 D8 `용인 14:00`
- 엑셀 갑지 E8 통행료 `2,900`
- 엑셀 갑지 합계 `52,900`
- 날짜 표시 형식 `yyyy-mm-dd aaa`
- 상세내역 RGB 색상 유지
- v51.91 좌우 폭 조절 및 동적 열 유지
- 적용 파일 3개 자동 백업 및 교체
- ZIP 내부 파일 경로 및 무결성 검사

## DB

- 새 컬럼 없음
- Supabase SQL 없음
- 통행료 행의 km와 유류비는 기존 `description`에 버전 구분이 있는 호환 형식으로 저장
