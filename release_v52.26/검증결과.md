# v52.26 검증결과

GitHub main 기준:
- `src/page/AttendanceWorkerPortal.jsx`
- Git blob SHA: `301eb1bada1ec7c1b2d66e354be48552432087ca`

확인한 기존 구조:
- QR 버튼에서 `navigator.mediaDevices.getUserMedia()` 호출
- stream 획득 후 `setScannerOpen(true)`
- scanner 화면은 후속 useEffect에서 video stream 연결
- 기존 NotAllowedError는 toast 안내만 표시

v52.26:
- Permissions API 지원 시 camera state 사전 확인
- 미허용/알 수 없음 상태에서 앱 자체 설명 Dialog 표시
- 사용자 `카메라 사용 허용` 클릭으로 getUserMedia 실행
- 브라우저가 이미 차단한 상태면 scanner 화면을 열지 않고 설정 안내
- Permissions API 미지원 브라우저에서도 getUserMedia 자체 권한 요청은 계속 사용
- 기존 stream 성공 후 scanner open 순서 유지
- SQL 변경 없음

발표용 Worker route는 `src/utils/attendance.js`의
`buildAttendanceWorkerUrl()`과 동일하게
현재 `window.location.origin` + `?view=attendance-worker` 구조입니다.
