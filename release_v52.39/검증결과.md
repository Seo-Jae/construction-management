# v52.39 검증결과

## 최신 main 확인

v52.38.1 실제 반영 확인.

`src/page/MonthlyLaborManagement.jsx`
SHA:
`8fa2d7d52cb9e5a01f8fc2d24540571d86281a62`

`src/components/LaborSecurityPhoneDialog.jsx`
존재 및 v52.38 RPC 연결 확인.

## 권한

preflight/request/verify 모두 서버에서 `auth.uid()`를 사용합니다.

현장 권한은 `labor_permission_allowed_v52_33(..., 'labor.cost.view', project)`로 재검증합니다.

최고관리자도 OTP 우회 경로를 만들지 않았습니다.

## 보안

- OTP 평문 DB 저장 없음
- keyed HMAC
- Vault secret
- 5분 만료
- 최대 5회
- snapshot binding
- verified phone fingerprint binding
- 보안휴대폰 변경 시 기존 challenge 취소
- 명단/노임/개인정보 수정 시 기존 challenge 취소
- audit에 OTP 원문 미기록
- challenge/audit direct table 접근 revoke + RLS

## SENS

현재 provider function은 의도적으로 `false`.

따라서 실제 SMS를 보냈다고 주장하지 않으며 challenge 생성도 차단됩니다.

## 프로그램

- 기존 v52.38.1 기능 전체 보존
- 신규 `LaborDownloadAuthDialog.jsx`
- MonthlyLaborManagement에는 import/state/button/dialog 연결만 추가

## 정적 검증

- apply script Node syntax
- Monthly JSX parser
- DownloadAuth JSX parser
- SQL 핵심 marker
- ZIP integrity

실제 Supabase SQL 실행과 Vite build는 사용자 프로젝트에서 최종 확인합니다.
