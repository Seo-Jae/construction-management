# v52.48.5.44.57 적용방법

프로젝트 최상위 폴더에서 실행합니다.

```powershell
node .\release_v52.48.5.44.57\apply-v52.48.5.44.57-guide-wysiwyg.cjs
npm run build
git add .
git commit -m "v52.48.5.44.57 가이드 편집/표시 WYSIWYG 보정"
git push origin main
```

`main` Push로 Vercel 운영 배포가 자동 시작되면 별도 `npx vercel --prod`는 실행하지 않아도 됩니다.
자동 배포가 시작되지 않는 경우에만 `npx vercel --prod`를 실행합니다.

추가 Supabase SQL은 없습니다.
