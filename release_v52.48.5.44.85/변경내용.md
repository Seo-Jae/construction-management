# v52.48.5.44.85 변경내용

대상: `src/components/Sidebar.jsx`

`건의·오류 제보/관리` 메뉴 아이콘만 다른 사이드 메뉴보다
왼쪽으로 튀어나오고 크기가 달라 보이는 문제를 수정합니다.

원인:
- 다른 주요 사이드 메뉴의 `ListItemIcon`은 `justifyContent: 'center'` 사용
- 건의·오류 메뉴만 해당 정렬값이 빠져 있었음
- 아이콘 크기도 `sx={{ fontSize: 19 }}`로 별도 지정되어 있었음

수정:
- `ListItemIcon`에 `justifyContent: 'center'` 추가
- `FeedbackOutlinedIcon`을 다른 메뉴와 동일하게 `fontSize="small"` 사용
- 메뉴 높이/텍스트/클릭영역은 변경하지 않음

추가 Supabase SQL 없음.
