# v52.48.5.44.28 변경내용

- MUI 9의 `MenuItem`이 `MenuList` 없이 사용되어 발생한 `MenuListContext is missing` 오류를 수정했습니다.
- 표시 옵션 목록을 `Paper → MenuList → MenuItem`의 정식 구조로 변경했습니다.
- 표시 옵션을 눌러도 화면이 백색으로 중단되지 않습니다.
- 선택박스 바로 아래 목록 표시, 최대 10행, 이후 내부 스크롤 기능은 그대로 유지합니다.
- 데이터베이스 구조 변경은 없습니다.
