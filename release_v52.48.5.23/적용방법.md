# v52.48.5.23 적용방법

적용 기준은 `v52.48.5.22`입니다. 이번 버전은 자재 검색 동의어만 확장하며 SQL 실행은 필요하지 않습니다.

## 1. 코드 적용

프로젝트 최상위 폴더에서 압축을 해제한 뒤 PowerShell에서 실행합니다.

```powershell
node .\release_v52.48.5.23\apply-v52.48.5.23-material-aliases.cjs
npm run build
```

## 2. Git 반영

```powershell
git add src/page/UnitPriceAnalysis.jsx
git status
git commit -m "v52.48.5.23 expand material search aliases"
git push origin main
```

배포 후 `런너`, `타일판`, `루바`, `볼트`, `클립바` 등 한글 검색어로 영문 자재가 조회되는지 확인합니다.
