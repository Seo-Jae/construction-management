# 검증결과

- 현재 GitHub main `src/page/HouseholdQuantityManagement.jsx`의
  `aria-label="골구도 표시 공정 선택"` Tabs 블록을 기준으로 패치 작성
- 기존 골구도 Tabs가 MUI 기본 indicator를 사용 중인 구조 확인
- 수정 후 `.MuiTabs-indicator { display: none }` 포함 확인
- 수정 후 선택 Tab 자체의 `Mui-selected::after` 밑줄 포함 확인
- 적용 스크립트 `node --check` 통과
- 더미 파일 기준 1회 적용 및 재적용(`[이미 적용됨]`) 확인
- 다른 공정 탭 및 골구도 데이터/줌/PDF/프린트 로직은 변경하지 않음
- 추가 Supabase SQL 없음
- 전체 Vite build / 실제 브라우저 90% 화면 렌더링은 이 패키지 생성 환경에서 수행하지 않음
