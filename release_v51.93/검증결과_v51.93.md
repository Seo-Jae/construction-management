# 지출결의서 작성 v51.93 검증결과

## 자동 검증

- `서울→용인 59km(유류비) / 용인 05:44(통행료)`가 `서울→용인(유류비)`로 변환되는지 확인
- `용인→서서울 59km(유류비) / 서서울 18:41(통행료)`가 `용인→서서울(유류비)`로 변환되는지 확인
- 갑지의 경로·km, 도착지·시간, 유류비·통행료 금액 및 전체 합계 유지 확인
- 상세내역 문구의 줄바꿈 해제 확인
- 작성월 밖 날짜 영역 6개 줄 전체가 `#D9D9D9`로 통일되는지 확인
- 평일 `#FFFFFF`, 주말 `#F9F9E1`, 제목·요일 `#12346C` 유지 확인
- 엑셀 파일 생성 후 재열기 시 문구·금액·색상 유지 확인
- 적용 스크립트가 `expenseResolutionExcel.js` 1개만 복사하고 기존 파일 1개를 자동 백업하는지 확인

## 실제 엑셀 화면 검증

- 두 시트의 주요 셀값·서식·수식 오류를 검사함
- 갑지와 상세내역을 각각 렌더링해 배치 확인
- 상세내역의 월 밖 칸이 줄 단위로 끊기지 않고 한 가지 회색으로 표시되는지 확인
- 상세내역의 이동 건이 한 줄씩 분리되어 표시되는지 확인

## 결과

검증 항목을 모두 통과했습니다.
