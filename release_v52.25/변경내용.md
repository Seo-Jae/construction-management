# v52.25 변경내용

대상:
- `src/page/AttendanceWorkerPortal.jsx`

변경:
- `MobileShell`에 `headerAction` 영역 추가
- 작업자 로그인 완료 화면에서 `headerAction`으로 로그아웃 IconButton 전달
- 헤더 Toolbar를 좌측 브랜드 / 우측 액션 구조로 변경
- 기존 하단 로그아웃 Button 제거
- 비설치 브라우저의 `앱으로 설치` 버튼은 하단에 유지

로그아웃 처리 함수 `handleLogout` 자체는 변경하지 않습니다.
따라서 기존 세션 삭제 및 로그아웃 동작은 그대로 사용합니다.

SQL 변경 없음.
