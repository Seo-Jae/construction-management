# v52.27 검증결과

확인된 최신 코드 구조:
- `loadMe` 내부에서 `setWorker(nextWorker)` 실행
- `loadMe` dependency에 `worker` 포함
- 초기 session effect가 `[loadMe]`에 의존
- scanner effect가 `processQrToken`에 의존
- `processQrToken`이 `loadMe`에 의존
- scanner cleanup에서 `stopScanner()` 실행

따라서 worker 객체 갱신이 연쇄적으로 scanner effect cleanup까지
유발할 수 있는 dependency 경로가 존재했습니다.

v52.27 검증:
- `workerRef` 존재
- `loadMe` dependency에서 worker 제거
- stale `return worker` 제거
- 기존 카메라 permission dialog 유지
- 기존 QR scanner 및 processQrToken 유지
- v52.23 티커 속도 유지
- v52.25 상단 로그아웃 유지
- 적용 전 자동백업
- 중복적용 감지
- SQL 변경 없음
