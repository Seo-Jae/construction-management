# v52.27 변경내용

대상:
- `src/page/AttendanceWorkerPortal.jsx`

수정:
- `workerRef` 도입
- `loadMe`의 dependency에서 `worker` 제거
- `loadMe` 일시 오류 반환값을 `workerRef.current`로 변경
- `setWorker(nextWorker)` 직전 `workerRef.current = nextWorker`
- token 없음/세션 무효/로그아웃 시 workerRef 초기화

목적:
- 세션 조회 무한 재호출 가능성 제거
- scanner useEffect의 dependency 연쇄 재생성 방지
- camera stream이 effect cleanup의 `stopScanner()`로 반복 종료되는 문제 차단

유지:
- v52.20 로그인 유지
- v52.21~23 공지/티커
- v52.25 상단 로그아웃
- v52.26 카메라 권한 안내
- QR 서버 처리 RPC

SQL 변경 없음.
