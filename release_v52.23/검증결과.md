# v52.23 검증결과

GitHub main 최신 기준 `src/page/AttendanceWorkerPortal.jsx`:
- Git blob SHA: `9d8d5b700cb26c16069903fdf89259b3ae9bcdfa`

확인된 기존 속도 계산:
```text
Math.max(
  20,
  Math.min(70, Math.round((contentLength + spacingWeight) * 0.42))
)
```

v52.23:
```text
Math.max(
  14.3,
  Math.min(50, Math.round((contentLength + spacingWeight) * 0.30))
)
```

검증:
- 적용 스크립트 Git blob SHA 보호
- 속도 기준 문자열 정확히 1회인지 검사
- 공지 간격 `gap: appMode ? 8 : 6` 유지
- 글씨크기 유지
- 애니메이션 방향 유지
- 적용 전 자동 백업
- 중복 적용 감지
- SQL 변경 없음
