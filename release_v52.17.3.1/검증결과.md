# 검증결과

- 적용 전 v52.17.2 기준 문자열 확인
- 대상 문자열 개수 검증
- 백업 폴더 `src/page`까지 재귀 생성
- 같은 버전 재실행 시 이미 적용 여부 감지
- ZIP 무결성 검사
- `qrcode`는 package.json에 이미 선언되어 있으므로 `npm install --legacy-peer-deps`로 로컬 node_modules를 복구
