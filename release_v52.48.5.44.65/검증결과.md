# 검증결과

- `.64` 기준 5개 대상 파일 SHA256 기준 검증 포함
- 1차 적용 후 대상 파일 SHA256 검증
- 동일 패치 재적용 시 `이미 적용됨` 처리
- `guideCatalog.js`, `guideAnnotationEditor.js`, `systemGuidePopup.js` `node --check` 통과
- 번호 숨김 샘플 정규화 테스트: `showNumber=false` 또는 `hideNumber=true` → `showNumber=false`, `hideNumber=true`, `number=0` 확인
- 순수 `number` 타입은 숨김 요청이 있어도 번호 표시 유지 확인
