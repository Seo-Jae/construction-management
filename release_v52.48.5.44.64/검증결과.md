# v52.48.5.44.64 검증결과

- v52.48.5.44.63 가이드 파일 5개 기준 적용 테스트: 통과
- 동일 패치 재적용(idempotent) 테스트: 통과
- `guideCatalog.js`, `guideAnnotationEditor.js`, `systemGuidePopup.js` JavaScript 구문 검사: 통과
- `GuideAnnotatedImage.jsx`, `Guide.jsx` JSX 파싱/변환 검사(TypeScript transpile): 통과
- `showNumber` 데이터 정규화 테스트:
  - 동그라미 `showNumber:false` 유지: 통과
  - 기존 데이터(showNumber 없음) 번호 표시 기본값 유지: 통과
  - 순수 번호 도구는 번호 표시 유지: 통과
  - 설명박스는 번호 없음 유지: 통과
- 번호 숨김 표시는 `numbered()`에서 제외되므로 번호 재정렬/번호 연결 선택 대상에서 제외
- 번호를 끄는 순간 기존 번호 연결 화살표가 해당 표시를 참조하면 함께 제거하도록 처리
- 전체 Vite `npm run build`는 현재 검증 컨테이너에 프로젝트 의존성이 설치되어 있지 않아 사용자 프로젝트에서 최종 확인 필요
- 추가 Supabase SQL 없음
