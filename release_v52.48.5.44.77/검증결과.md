# 검증결과

- 현재 GitHub main `src/page/MaterialInputStatus.jsx` 기준으로 패치 작성
- 기존 다운로드 구조 확인:
  - tabValue 1 = 업체별 월·누계 Workbook
  - tabValue 0/2 = 동일 품목별 집계 Workbook
- 업체별 Workbook에는 기존처럼 `업체별 월누계` + `월 원본 상세` 2개 시트 유지
- Excel 공통 문서서식 함수 삽입 확인
- 금액/수량/건수 number format 적용 코드 확인
- A4 landscape / fitToWidth=1 / printArea / printTitlesRow 설정 포함 확인
- 탭 variant standard 및 overflow visible 보정 확인
- 적용 스크립트 `node --check` 통과
- 동일 패치 재적용 시 `[이미 적용됨]` 처리 확인
- 전체 Vite build 및 브라우저 실제 다운로드 파일 열기 테스트는 이 패키지 생성 환경에서 수행하지 않음
- 추가 Supabase SQL 없음
