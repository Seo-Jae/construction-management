# v52.48.2.1 적용방법

v52.48.2가 아래 오류로 중단된 경우 사용하는 핫픽스입니다.

`휴대폰 입력 확대: 기준 코드가 2개 이상 발견되었습니다.`

v52.48.2는 실제 파일 저장 전에 중단되므로
별도 복구 없이 이 버전을 바로 실행하면 됩니다.

## SQL

변경 없습니다.

## 적용

```powershell
node .\release_v52.48.2.1\apply-v52.48.2.1-attendance-login-scale-hotfix.cjs
npm run build
```

정상이라면:

```text
[v52.48.2.1 적용 완료]
- v52.48.2 중복 기준코드 오류 수정
- 휴대폰번호/비밀번호를 각각 anchor 기준으로 안전 수정
...
```

가 표시됩니다.

## Git

빌드 성공 후:

```powershell
git add src/page/AttendanceWorkerPortal.jsx
git status
git commit -m "v52.48.2.1 scale up attendance login ui"
git push origin main
```

`git add .`은 사용하지 않습니다.

기준 main SHA:
`ec519253552588be530818f66a2ef3f2f307a5cd`
