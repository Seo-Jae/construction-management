const fs = require('fs');
const path = require('path');

const VERSION = 'v52.48.5.44.74';
const projectRoot = process.cwd();
const targetPath = path.join(projectRoot, 'src', 'page', 'ReportDocumentList.jsx');
const replacements = [{"label": "MUI Dialog import 추가", "oldText": "  CircularProgress,\n  Fade,\n  IconButton,", "newText": "  CircularProgress,\n  Dialog,\n  DialogActions,\n  DialogContent,\n  DialogTitle,\n  Fade,\n  IconButton,"}, {"label": "일시 표시 함수 추가", "oldText": "const headerCellSx = {", "newText": "const formatDateTime = (value) => {\n  if (!value) return '-';\n\n  const date = new Date(value);\n  if (Number.isNaN(date.getTime())) return String(value);\n\n  return new Intl.DateTimeFormat('ko-KR', {\n    timeZone: 'Asia/Seoul',\n    year: 'numeric',\n    month: '2-digit',\n    day: '2-digit',\n    hour: '2-digit',\n    minute: '2-digit',\n  }).format(date);\n};\n\nconst headerCellSx = {"}, {"label": "반려의견 선택 상태 추가", "oldText": "  const [deleting, setDeleting] = useState(false);\n\n  useEffect(() => {", "newText": "  const [deleting, setDeleting] = useState(false);\n  const [rejectionDocument, setRejectionDocument] = useState(null);\n\n  useEffect(() => {"}, {"label": "반려 상태 Chip 클릭 처리", "oldText": "                      <TableCell align=\"center\" sx={bodyCellSx}>\n                        <Chip\n                          size=\"small\"\n                          label={status.label}\n                          sx={{ height: 22, color: status.color, bgcolor: status.bgcolor, fontSize: '0.62rem', fontWeight: 900 }}\n                        />\n                      </TableCell>", "newText": "                      <TableCell\n                        align=\"center\"\n                        sx={bodyCellSx}\n                        onClick={(event) => {\n                          if (\n                            reportType === 'proposal' &&\n                            document.status === 'rejected'\n                          ) {\n                            event.stopPropagation();\n                          }\n                        }}\n                      >\n                        <Tooltip\n                          title={\n                            reportType === 'proposal' &&\n                            document.status === 'rejected'\n                              ? '반려 의견 보기'\n                              : ''\n                          }\n                          disableHoverListener={\n                            !(\n                              reportType === 'proposal' &&\n                              document.status === 'rejected'\n                            )\n                          }\n                        >\n                          <Chip\n                            size=\"small\"\n                            label={status.label}\n                            onClick={\n                              reportType === 'proposal' &&\n                              document.status === 'rejected'\n                                ? (event) => {\n                                    event.stopPropagation();\n                                    setRejectionDocument(document);\n                                  }\n                                : undefined\n                            }\n                            sx={{\n                              height: 22,\n                              color: status.color,\n                              bgcolor: status.bgcolor,\n                              fontSize: '0.62rem',\n                              fontWeight: 900,\n                              cursor:\n                                reportType === 'proposal' &&\n                                document.status === 'rejected'\n                                  ? 'pointer'\n                                  : 'default',\n                              '&:hover':\n                                reportType === 'proposal' &&\n                                document.status === 'rejected'\n                                  ? {\n                                      bgcolor: '#fecaca',\n                                      boxShadow:\n                                        '0 0 0 1px rgba(185,28,28,0.18)',\n                                    }\n                                  : undefined,\n                            }}\n                          />\n                        </Tooltip>\n                      </TableCell>"}, {"label": "반려의견 Dialog 추가", "oldText": "      <ApprovalReportViewer\n        open={Boolean(previewRequest)}\n        request={previewRequest}\n        onClose={() => setPreviewDocument(null)}\n      />\n    </Box>", "newText": "      <Dialog\n        open={Boolean(rejectionDocument)}\n        onClose={() => setRejectionDocument(null)}\n        fullWidth\n        maxWidth=\"sm\"\n      >\n        <DialogTitle\n          sx={{\n            pb: 1,\n            color: '#991b1b',\n            fontSize: '1rem',\n            fontWeight: 900,\n          }}\n        >\n          반려 의견\n        </DialogTitle>\n\n        <DialogContent dividers sx={{ py: 2 }}>\n          {rejectionDocument && (\n            <>\n              <Box sx={{ mb: 1.5 }}>\n                <Typography\n                  sx={{\n                    color: '#0f172a',\n                    fontSize: '0.82rem',\n                    fontWeight: 900,\n                  }}\n                >\n                  {rejectionDocument.title || '품의 보고'}\n                </Typography>\n                <Typography\n                  sx={{\n                    mt: 0.25,\n                    color: '#64748b',\n                    fontSize: '0.7rem',\n                  }}\n                >\n                  작성자: {rejectionDocument.author_name || '-'}\n                  {rejectionDocument.author_position\n                    ? ` · ${rejectionDocument.author_position}`\n                    : ''}\n                </Typography>\n              </Box>\n\n              {(rejectionDocument.approval_steps || [])\n                .filter((step) => step.status === 'rejected')\n                .map((step, index) => (\n                  <Paper\n                    key={step.id || `rejected-step-${index}`}\n                    variant=\"outlined\"\n                    sx={{\n                      mb: 1,\n                      p: 1.5,\n                      borderColor: '#fecaca',\n                      bgcolor: '#fff7f7',\n                      boxShadow: 'none',\n                    }}\n                  >\n                    <Typography\n                      sx={{\n                        color: '#b91c1c',\n                        fontSize: '0.76rem',\n                        fontWeight: 900,\n                      }}\n                    >\n                      {step.approver_name || '결재자'}\n                      {step.approver_position\n                        ? ` · ${step.approver_position}`\n                        : ''}\n                    </Typography>\n                    <Typography\n                      sx={{\n                        mt: 0.2,\n                        color: '#94a3b8',\n                        fontSize: '0.66rem',\n                      }}\n                    >\n                      반려일시: {formatDateTime(step.acted_at)}\n                    </Typography>\n\n                    <Typography\n                      sx={{\n                        mt: 1.2,\n                        color: '#475569',\n                        fontSize: '0.7rem',\n                        fontWeight: 800,\n                      }}\n                    >\n                      수정·보완 요청사항\n                    </Typography>\n                    <Box\n                      sx={{\n                        mt: 0.45,\n                        p: 1.1,\n                        minHeight: 54,\n                        border: '1px solid #fee2e2',\n                        borderRadius: 1,\n                        bgcolor: '#ffffff',\n                      }}\n                    >\n                      <Typography\n                        sx={{\n                          color: '#7f1d1d',\n                          fontSize: '0.78rem',\n                          lineHeight: 1.65,\n                          whiteSpace: 'pre-wrap',\n                          wordBreak: 'break-word',\n                        }}\n                      >\n                        {String(step.comment || '').trim() ||\n                          '등록된 반려 의견이 없습니다.'}\n                      </Typography>\n                    </Box>\n                  </Paper>\n                ))}\n\n              {(rejectionDocument.approval_steps || []).filter(\n                (step) => step.status === 'rejected',\n              ).length === 0 && (\n                <Alert severity=\"warning\">\n                  반려 상태는 확인되지만 저장된 반려 의견을 찾지 못했습니다.\n                  목록을 새로고침한 뒤 다시 확인해주세요.\n                </Alert>\n              )}\n            </>\n          )}\n        </DialogContent>\n\n        <DialogActions>\n          <Button\n            onClick={() => setRejectionDocument(null)}\n            sx={{ fontWeight: 800 }}\n          >\n            닫기\n          </Button>\n        </DialogActions>\n      </Dialog>\n\n      <ApprovalReportViewer\n        open={Boolean(previewRequest)}\n        request={previewRequest}\n        onClose={() => setPreviewDocument(null)}\n      />\n    </Box>"}];

if (!fs.existsSync(targetPath)) {
  console.error(`[적용 중단] 파일을 찾을 수 없습니다: ${targetPath}`);
  process.exit(1);
}

let source = fs.readFileSync(targetPath, 'utf8');
let changed = false;

for (const replacement of replacements) {
  if (source.includes(replacement.newText)) {
    console.log(`[이미 적용됨] ${replacement.label}`);
    continue;
  }

  if (!source.includes(replacement.oldText)) {
    console.error(`[적용 중단] ${replacement.label} 위치가 현재 파일과 다릅니다.`);
    console.error('기존 변경을 보호하기 위해 자동 덮어쓰기를 하지 않았습니다.');
    process.exit(1);
  }

  source = source.replace(replacement.oldText, replacement.newText);
  changed = true;
  console.log(`[적용] ${replacement.label}`);
}

if (!changed) {
  console.log('\n전체 변경이 이미 적용되어 있습니다.');
  process.exit(0);
}

const stamp = new Date().toISOString().replace(/[:.]/g, '-');
const backupPath = `${targetPath}.bak-${VERSION}-${stamp}`;
fs.copyFileSync(targetPath, backupPath);
fs.writeFileSync(targetPath, source, 'utf8');

console.log(`\n[적용 완료] ${path.relative(projectRoot, targetPath)}`);
console.log(`[백업] ${path.relative(projectRoot, backupPath)}`);
console.log('품의 보고 목록의 반려 상태를 클릭하면 결재자의 반려 의견을 확인할 수 있습니다.');
