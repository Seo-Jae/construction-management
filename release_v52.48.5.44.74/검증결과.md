# 검증결과

- 현재 GitHub main의 `src/page/ReportDocumentList.jsx` 구조를 기준으로 패치 작성
- `fetchReportDocuments()`가 현재 결재차수의 `approval_steps`를 문서별로 포함하며,
  각 단계에 `status`, `acted_at`, `comment`, `approver_name`, `approver_position`이 포함되는 구조 확인
- 적용 스크립트 `node --check` 통과
- 동일 패치 재실행 시 이미 적용된 블록은 건너뛰도록 구성
- Supabase SQL 변경 없음
- 전체 Vite build는 패키지 생성 환경에서는 실행하지 않음
