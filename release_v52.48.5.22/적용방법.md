# v52.48.5.22 적용방법

적용 기준은 `v52.48.5.21`입니다. 이번 버전은 자재 검색과 팝업 화면만 변경하며 SQL 실행은 필요하지 않습니다.

## 1. 코드 적용

프로젝트 최상위 폴더에서 압축을 해제한 뒤 PowerShell에서 실행합니다.

```powershell
node .\release_v52.48.5.22\apply-v52.48.5.22-material-search.cjs
npm run build
```

## 2. Git 반영

```powershell
git add src/page/UnitPriceAnalysis.jsx
git status
git commit -m "v52.48.5.22 improve material search"
git push origin main
```

배포 후 자재 추가 팝업에서 `stud`, `스터드`, `stud50`, `스터드50형`을 각각 검색하고, 결과가 없어도 팝업 높이가 유지되는지 확인합니다.
