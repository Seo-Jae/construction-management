# v52.22 검증결과

기준 GitHub main:
- 최신 커밋: `bd2fd32805803beee5f4a561d7f493675c7922bd`
- 커밋 메시지: `v52.21 attendance notice ordering`
- `src/page/AttendanceManagement.jsx`
- Git blob SHA: `28f573c7c53e90613d379dc0d3ad601bd5566905`

참고 UI:
- `src/page/LaborCostManagement.jsx`의 공정별 노임작성에서
  `+ / − / ↑ / ↓ / 선택 N개` 및 체크 선택 후 순서 이동 방식을 확인하여 동일한 조작 방식으로 설계

적용 스크립트:
- v52.21 Git blob SHA 일치 시에만 실행
- 모든 교체 문자열을 검증한 뒤 실제 파일 저장
- 적용 전 `backup_v52.22_...` 자동 백업
- 중복 적용 감지
- `표시 순번` 입력란이 남아 있으면 적용 중단
- 신규 RPC/체크박스/이동버튼/삭제창 마커 검증

SQL:
- 순서 저장 시 해당 현장의 전체 공지 ID 배열인지 검증
- 삭제 시 선택 ID가 해당 현장 공지인지 검증
- 쓰기 작업 전 기존 v52.17 저장 RPC를 동일 값으로 호출해 기존 수정권한/현장권한 검증 재사용
- 삭제 후 자동 순번 정규화

실제 Supabase SQL 실행과 Vite Production build는 사용자 운영환경에서 최종 확인합니다.
