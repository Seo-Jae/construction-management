# v52.48.5.44.50 검증결과

## 적용 검증
- 기준 `v52.48.5.44.49` 복사본에 적용 스크립트 정상 적용
- 동일 복사본에 재적용 2회 정상 통과(멱등성 확인)
- 적용 대상 외 파일은 변경하지 않음

## 구문 검증
- `src/config/guideCatalog.js` Node 구문검사 통과
- `src/utils/systemGuidePopup.js` Node 구문검사 통과
- `src/utils/guideAnnotationEditor.js` Node 구문검사 통과
- 편집기 팝업 내부에 생성되는 인라인 JavaScript를 별도로 추출하여 구문검사 통과
- `src/components/GuideAnnotatedImage.jsx` TypeScript JSX 파서 기준 구문검사 통과

## 기능 로직 검증
- 신규 점선박스/동그라미의 기본 번호 부착 위치 `우측상단` 확인
- 번호 연결 화살표 테스트: 1번 중심 40 → 2번 중심 80 조건에서 실제 선은 약 41.8 → 76.6으로 계산되어 시작/도착 번호 외곽에 여백이 생기는 것 확인
- 고정 위치 번호는 도형 좌표/크기를 기준으로 계산되어 이동/리사이즈 시 자동 추적하도록 구성
- `자유` 선택 또는 번호 원 직접 드래그 시 독립 좌표 저장하도록 구성
- 점선박스/동그라미 선택 시 8방향 리사이즈 핸들 생성 로직 확인
- 설명박스 자동 번호 접두어 제거 및 자동 폭 CSS 적용 확인
- 줌/팬: 빈 화면 드래그, 휠 줌, +/- 줌, 맞춤 기능 포함 확인

## 전체 Vite build
현재 검증용 복사본의 `node_modules`가 완전한 설치본이 아니어서 이 환경에서는 `vite` 실행 파일을 사용할 수 없었습니다. 실제 개발 PC에서는 적용 후 `npm run build`를 실행해 최종 빌드를 확인하십시오.

## DB
추가 Supabase SQL 없음.
